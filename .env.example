# Copy to .env and correct if necessary
# Скопируйте в .env и при необходимости измените
HOST=127.0.0.1
PORT=8080
BASE_URL=http://127.0.0.1:8080
# SQLite by default. For Postgres, see the example below.
# SQLite по умолчанию. Для Postgres пример ниже.
DATABASE_URL=sqlite:///./data.db

# For Postgres, for example:
# Для Postgres, например:
# DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/shortener